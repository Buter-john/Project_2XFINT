node_modules
# Keep environment variables out of version control
.env

/src/generated/prisma

# Fichiers générés par des outils IA (Windsurf), pas liés au projet
.windsurf/
skills-lock.json

# Fichiers uploades par les utilisateurs (justificatifs), pas du code source
uploads/*
!uploads/.gitkeep
